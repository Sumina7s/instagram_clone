import 'package:flutter/material.dart';
class ResponssiveLayout extends StatelessWidget {
  const ResponssiveLayout({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}